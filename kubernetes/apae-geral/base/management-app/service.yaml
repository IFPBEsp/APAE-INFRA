apiVersion: v1
kind: Service
metadata:
  name: management-app
  labels:
    app: management-app
spec:
  type: ClusterIP
  selector:
    app: management-app
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP